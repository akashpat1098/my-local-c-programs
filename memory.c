#include <stdio.h>
#include <stdlib.h>
#include<string.h>
int main()
{   
   

    // int *ptr;
    // int n;
                //use of malloc()
    // printf("Enter the size of array\n");
    // scanf("%d", &n);
    // ptr = (int *)malloc(n * sizeof(int));
    // for (int i = 0; i < n; i++)
    // {
    //     printf("Enter the value no %d of this array\n", i);
    //     scanf("%d", &ptr[i]);
    // }
    // for (int i = 0; i < n; i++)
    // {
    //     printf("The value at %d index of this array is %d\n", i, ptr[i]);
    // }

                //use of calloc()

    // printf("Enter the size of array\n");
    // scanf("%d", &n);
    // ptr = (int *)calloc(n, sizeof(int));
    // for (int i = 0; i < n; i++)
    // {
    //     printf("Enter the value no %d of this array\n", i);
    //     scanf("%d", &ptr[i]);
    // }
    // for (int i = 0; i < n; i++)
    // {
    //     printf("The value at %d index of this array is %d\n", i, ptr[i]);
    // }

    return 0;
}